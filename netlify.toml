[build]
    publish = "dist"
    command = "npm run build"

[dev]
    command = "npm run dev"
    port = 8888

[functions]
    directory = "netlify/functions"

[redirects]
    [[redirects]]
        from = "/api/*"
        to = "/.netlify/functions/:splat"
        status = 200